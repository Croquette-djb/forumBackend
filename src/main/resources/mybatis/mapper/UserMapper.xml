<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forum.forumbackend.mapper.UserMapper">
    <select id="findUserByUserName" resultType="com.forum.forumbackend.bean.User">
        select * from users where userName=#{userName}
    </select>
<!--    <select id="findUserByUserId" resultType="com.forum.forumbackend.bean.User">-->
<!--        select * from users where userId=#{useId}-->
<!--    </select>-->
</mapper>
